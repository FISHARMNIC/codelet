Shows pointers etc.
Uses WASM

todos:

function callable in c like js_showstring() and the memory like highlights it

DO VARIABLES ON STACK HAVE THEIR CHANGES CAPTURED? maybe need second array for stack then concat

errors in compiler dont unlock editor

siders/numbers to set memory inspection size